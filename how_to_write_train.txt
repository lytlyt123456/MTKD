if args.myarg:
如果args.myarg是字符串类型的参数，则如果这个字符串参数的值是非空串则条件判断为True，若是空串或者是None则条件判断为False；
如果args.myarg是action=’store_true’的参数，则如果命令行里有这个参数，则条件判断为True，如果命令行里没有这个参数，则条件判断为False；
如果args.myarg是数值类型参数，则如果值非零则条件判断为True，如果值为零则条件判断为False。

<1> import 所有注册的Dataset所在的文件和所有注册的Trainer所在的文件
<2> 如果根据需要新加入了其他的argument，就在reset_cfg()里根据argument改cfg ->
if args.second_phase:
	cfg.TRAINER.SECOND_PHASE = True
<3> extend_cfg() ->
from yacs.config import CfgNode as CN
cfg.TRAINER.MYTRAINER = CN()
cfg.TRAINER.MYTRAINER.___ = ___ # 关于自己的Trainer所需的一些配置参数，也就是注册的TrainerX类中用到的cfg.TRAINER.MYTRAINER中的参数，和自己的训练方法有关的参数都可以放在这里
定义Trainer和Dataset的配置文件
<4> main()里加入扩展的内容
<5> if __name__ == "__main__": 根据需要新加入一些args
